# CI Skills — skill-linter untuk seluruh skills/ (ROADMAP §7.1, §32)
# Semua skill wajib lolos linter otomatis sejak Phase 1.
name: ci-skills

on:
  push:
    paths:
      - "skills/**"
      - "knowledge/**"
      - "ROUTING.md"
      - "tools/skill-linter/**"
      - "tools/knowledge-linter/**"
      - "benchmarks/**"
      - "tests/test_skill_linter.py"
      - "tests/test_knowledge_linter.py"
      - "tests/benchmark/**"
      - ".github/workflows/ci-skills.yml"
  pull_request:
    paths:
      - "skills/**"
      - "knowledge/**"
      - "ROUTING.md"
      - "tools/skill-linter/**"
      - "tools/knowledge-linter/**"
      - "benchmarks/**"
      - "tests/test_skill_linter.py"
      - "tests/test_knowledge_linter.py"
      - "tests/benchmark/**"
      - ".github/workflows/ci-skills.yml"

permissions:
  contents: read

jobs:
  lint-skills:
    name: Skill linter (skills/)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Setup Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"

      - name: Jalankan skill-linter terhadap skills/
        run: python tools/skill-linter/lint.py skills/

      - name: Jalankan knowledge-linter terhadap knowledge/
        run: python tools/knowledge-linter/lint.py knowledge/

      - name: Jalankan seluruh Python regression tests
        run: python -m unittest discover -s tests -v
